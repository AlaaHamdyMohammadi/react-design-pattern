import React from 'react'

const ComponentHasError = () => {
  return (
    <div>
      this pattern for show the error messeges when something goes wrong in fallback component
    </div>
  )
}

export default ComponentHasError
